#ifndef ARMOR_H
#define ARMOR_H

#include "item.h"
#include <string>

class Armor : public Item {
 private:
  int defence;

 public:
  Armor(std::string, int);
  int GetDefence();

  void SetDefence(int);

  bool IsWearable();
};

#endif
