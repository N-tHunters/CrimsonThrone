#ifndef STACKABLEITEM_H
#define STACKABLEITEM_H

#include "item.h"
#include "actor.h"

class StackableItem : public Item {
 private:
  int count;
 public:
  int GetCount();
  void SetCount(int);
  bool IsStackable();
  void Use(Actor *);
  void Stack(StackableItem *);
};

#endif
